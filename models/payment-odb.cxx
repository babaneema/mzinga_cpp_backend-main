// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "payment-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // payments
  //

  const char alias_traits<  ::bill,
    id_mysql,
    access::object_traits_impl< ::payments, id_mysql >::pay_bill_tag>::
  table_name[] = "`pay_bill`";

  const char alias_traits<  ::customer,
    id_mysql,
    access::object_traits_impl< ::payments, id_mysql >::pay_customer_tag>::
  table_name[] = "`pay_customer`";

  struct access::object_traits_impl< ::payments, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::payments, id_mysql >::id_type
  access::object_traits_impl< ::payments, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::payments, id_mysql >::id_type
  access::object_traits_impl< ::payments, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.pay_id_value,
        i.pay_id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::payments, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // pay_id
    //
    t[0UL] = 0;

    // pay_unique
    //
    if (t[1UL])
    {
      i.pay_unique_value.capacity (i.pay_unique_size);
      grew = true;
    }

    // pay_bill
    //
    t[2UL] = 0;

    // pay_customer
    //
    t[3UL] = 0;

    // pay_method
    //
    if (t[4UL])
    {
      i.pay_method_value.capacity (i.pay_method_size);
      grew = true;
    }

    // pay_type
    //
    if (t[5UL])
    {
      i.pay_type_value.capacity (i.pay_type_size);
      grew = true;
    }

    // pay_amount
    //
    if (t[6UL])
    {
      i.pay_amount_value.capacity (i.pay_amount_size);
      grew = true;
    }

    // pay_reg_date
    //
    if (t[7UL])
    {
      i.pay_reg_date_value.capacity (i.pay_reg_date_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::payments, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // pay_id
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONGLONG;
      b[n].is_unsigned = 1;
      b[n].buffer = &i.pay_id_value;
      b[n].is_null = &i.pay_id_null;
      n++;
    }

    // pay_unique
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.pay_unique_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.pay_unique_value.capacity ());
    b[n].length = &i.pay_unique_size;
    b[n].is_null = &i.pay_unique_null;
    n++;

    // pay_bill
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.pay_bill_value;
    b[n].is_null = &i.pay_bill_null;
    n++;

    // pay_customer
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.pay_customer_value;
    b[n].is_null = &i.pay_customer_null;
    n++;

    // pay_method
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.pay_method_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.pay_method_value.capacity ());
    b[n].length = &i.pay_method_size;
    b[n].is_null = &i.pay_method_null;
    n++;

    // pay_type
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.pay_type_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.pay_type_value.capacity ());
    b[n].length = &i.pay_type_size;
    b[n].is_null = &i.pay_type_null;
    n++;

    // pay_amount
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.pay_amount_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.pay_amount_value.capacity ());
    b[n].length = &i.pay_amount_size;
    b[n].is_null = &i.pay_amount_null;
    n++;

    // pay_reg_date
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.pay_reg_date_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.pay_reg_date_value.capacity ());
    b[n].length = &i.pay_reg_date_size;
    b[n].is_null = &i.pay_reg_date_null;
    n++;
  }

  void access::object_traits_impl< ::payments, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::payments, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // pay_id
    //
    if (sk == statement_insert)
    {
      long unsigned int const& v =
        o.pay_id;

      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.pay_id_value, is_null, v);
      i.pay_id_null = is_null;
    }

    // pay_unique
    //
    {
      ::std::string const& v =
        o.pay_unique;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.pay_unique_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.pay_unique_value,
        size,
        is_null,
        v);
      i.pay_unique_null = is_null;
      i.pay_unique_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.pay_unique_value.capacity ());
    }

    // pay_bill
    //
    {
      ::boost::shared_ptr< ::bill > const& v =
        o.pay_bill;

      typedef object_traits< ::bill > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::bill > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.pay_bill_value, is_null, id);
        i.pay_bill_null = is_null;
      }
      else
        i.pay_bill_null = 1;
    }

    // pay_customer
    //
    {
      ::boost::shared_ptr< ::customer > const& v =
        o.pay_customer;

      typedef object_traits< ::customer > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::customer > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.pay_customer_value, is_null, id);
        i.pay_customer_null = is_null;
      }
      else
        i.pay_customer_null = 1;
    }

    // pay_method
    //
    {
      ::std::string const& v =
        o.pay_method;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.pay_method_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.pay_method_value,
        size,
        is_null,
        v);
      i.pay_method_null = is_null;
      i.pay_method_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.pay_method_value.capacity ());
    }

    // pay_type
    //
    {
      ::std::string const& v =
        o.pay_type;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.pay_type_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.pay_type_value,
        size,
        is_null,
        v);
      i.pay_type_null = is_null;
      i.pay_type_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.pay_type_value.capacity ());
    }

    // pay_amount
    //
    {
      ::std::string const& v =
        o.pay_amount;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.pay_amount_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.pay_amount_value,
        size,
        is_null,
        v);
      i.pay_amount_null = is_null;
      i.pay_amount_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.pay_amount_value.capacity ());
    }

    // pay_reg_date
    //
    {
      ::std::string const& v =
        o.pay_reg_date;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.pay_reg_date_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.pay_reg_date_value,
        size,
        is_null,
        v);
      i.pay_reg_date_null = is_null;
      i.pay_reg_date_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.pay_reg_date_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::payments, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // pay_id
    //
    {
      long unsigned int& v =
        o.pay_id;

      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        v,
        i.pay_id_value,
        i.pay_id_null);
    }

    // pay_unique
    //
    {
      ::std::string& v =
        o.pay_unique;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.pay_unique_value,
        i.pay_unique_size,
        i.pay_unique_null);
    }

    // pay_bill
    //
    {
      ::boost::shared_ptr< ::bill >& v =
        o.pay_bill;

      typedef object_traits< ::bill > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::bill > > ptr_traits;

      if (i.pay_bill_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.pay_bill_value,
          i.pay_bill_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // pay_customer
    //
    {
      ::boost::shared_ptr< ::customer >& v =
        o.pay_customer;

      typedef object_traits< ::customer > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::customer > > ptr_traits;

      if (i.pay_customer_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.pay_customer_value,
          i.pay_customer_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // pay_method
    //
    {
      ::std::string& v =
        o.pay_method;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.pay_method_value,
        i.pay_method_size,
        i.pay_method_null);
    }

    // pay_type
    //
    {
      ::std::string& v =
        o.pay_type;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.pay_type_value,
        i.pay_type_size,
        i.pay_type_null);
    }

    // pay_amount
    //
    {
      ::std::string& v =
        o.pay_amount;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.pay_amount_value,
        i.pay_amount_size,
        i.pay_amount_null);
    }

    // pay_reg_date
    //
    {
      ::std::string& v =
        o.pay_reg_date;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.pay_reg_date_value,
        i.pay_reg_date_size,
        i.pay_reg_date_null);
    }
  }

  void access::object_traits_impl< ::payments, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::payments, id_mysql >::persist_statement[] =
  "INSERT INTO `payments` "
  "(`pay_id`, "
  "`pay_unique`, "
  "`pay_bill`, "
  "`pay_customer`, "
  "`pay_method`, "
  "`pay_type`, "
  "`pay_amount`, "
  "`pay_reg_date`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::payments, id_mysql >::find_statement[] =
  "SELECT "
  "`payments`.`pay_id`, "
  "`payments`.`pay_unique`, "
  "`payments`.`pay_bill`, "
  "`payments`.`pay_customer`, "
  "`payments`.`pay_method`, "
  "`payments`.`pay_type`, "
  "`payments`.`pay_amount`, "
  "`payments`.`pay_reg_date` "
  "FROM `payments` "
  "WHERE `payments`.`pay_id`=?";

  const char access::object_traits_impl< ::payments, id_mysql >::update_statement[] =
  "UPDATE `payments` "
  "SET "
  "`pay_unique`=?, "
  "`pay_bill`=?, "
  "`pay_customer`=?, "
  "`pay_method`=?, "
  "`pay_type`=?, "
  "`pay_amount`=?, "
  "`pay_reg_date`=? "
  "WHERE `pay_id`=?";

  const char access::object_traits_impl< ::payments, id_mysql >::erase_statement[] =
  "DELETE FROM `payments` "
  "WHERE `pay_id`=?";

  const char access::object_traits_impl< ::payments, id_mysql >::query_statement[] =
  "SELECT\n"
  "`payments`.`pay_id`,\n"
  "`payments`.`pay_unique`,\n"
  "`payments`.`pay_bill`,\n"
  "`payments`.`pay_customer`,\n"
  "`payments`.`pay_method`,\n"
  "`payments`.`pay_type`,\n"
  "`payments`.`pay_amount`,\n"
  "`payments`.`pay_reg_date`\n"
  "FROM `payments`\n"
  "LEFT JOIN `bill` AS `pay_bill` ON `pay_bill`.`bill_id`=`payments`.`pay_bill`\n"
  "LEFT JOIN `customer` AS `pay_customer` ON `pay_customer`.`customer_id`=`payments`.`pay_customer`";

  const char access::object_traits_impl< ::payments, id_mysql >::erase_query_statement[] =
  "DELETE FROM `payments`";

  const char access::object_traits_impl< ::payments, id_mysql >::table_name[] =
  "`payments`";

  void access::object_traits_impl< ::payments, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im.pay_id_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.pay_id = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::payments, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.pay_id);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::payments, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::payments, id_mysql >::pointer_type
  access::object_traits_impl< ::payments, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::payments, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::payments, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.pay_id);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::payments, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::payments, id_mysql >::object_type >
  access::object_traits_impl< ::payments, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += "\n";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        true,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::payments, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
