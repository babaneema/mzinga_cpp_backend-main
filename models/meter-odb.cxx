// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "meter-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // meter
  //

  const char alias_traits<  ::customer,
    id_mysql,
    access::object_traits_impl< ::meter, id_mysql >::meter_customer_tag>::
  table_name[] = "`meter_customer`";

  struct access::object_traits_impl< ::meter, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::meter, id_mysql >::id_type
  access::object_traits_impl< ::meter, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::meter, id_mysql >::id_type
  access::object_traits_impl< ::meter, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.meter_id_value,
        i.meter_id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::meter, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // meter_id
    //
    t[0UL] = 0;

    // meter_unique
    //
    if (t[1UL])
    {
      i.meter_unique_value.capacity (i.meter_unique_size);
      grew = true;
    }

    // meter_customer
    //
    t[2UL] = 0;

    // meter_owner
    //
    if (t[3UL])
    {
      i.meter_owner_value.capacity (i.meter_owner_size);
      grew = true;
    }

    // meter_number
    //
    if (t[4UL])
    {
      i.meter_number_value.capacity (i.meter_number_size);
      grew = true;
    }

    // meter_intital_unit
    //
    if (t[5UL])
    {
      i.meter_intital_unit_value.capacity (i.meter_intital_unit_size);
      grew = true;
    }

    // meter_joinging_price
    //
    if (t[6UL])
    {
      i.meter_joinging_price_value.capacity (i.meter_joinging_price_size);
      grew = true;
    }

    // meter_lock
    //
    if (t[7UL])
    {
      if (mysql::enum_traits::grow (i.meter_lock_value, i.meter_lock_size))
        grew = true;
      else
        t[7UL] = 0;
    }

    // meter_in_service
    //
    if (t[8UL])
    {
      i.meter_in_service_value.capacity (i.meter_in_service_size);
      grew = true;
    }

    // meter_reg_date
    //
    if (t[9UL])
    {
      i.meter_reg_date_value.capacity (i.meter_reg_date_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::meter, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // meter_id
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONGLONG;
      b[n].is_unsigned = 1;
      b[n].buffer = &i.meter_id_value;
      b[n].is_null = &i.meter_id_null;
      n++;
    }

    // meter_unique
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.meter_unique_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.meter_unique_value.capacity ());
    b[n].length = &i.meter_unique_size;
    b[n].is_null = &i.meter_unique_null;
    n++;

    // meter_customer
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.meter_customer_value;
    b[n].is_null = &i.meter_customer_null;
    n++;

    // meter_owner
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.meter_owner_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.meter_owner_value.capacity ());
    b[n].length = &i.meter_owner_size;
    b[n].is_null = &i.meter_owner_null;
    n++;

    // meter_number
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.meter_number_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.meter_number_value.capacity ());
    b[n].length = &i.meter_number_size;
    b[n].is_null = &i.meter_number_null;
    n++;

    // meter_intital_unit
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.meter_intital_unit_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.meter_intital_unit_value.capacity ());
    b[n].length = &i.meter_intital_unit_size;
    b[n].is_null = &i.meter_intital_unit_null;
    n++;

    // meter_joinging_price
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.meter_joinging_price_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.meter_joinging_price_value.capacity ());
    b[n].length = &i.meter_joinging_price_size;
    b[n].is_null = &i.meter_joinging_price_null;
    n++;

    // meter_lock
    //
    mysql::enum_traits::bind (b[n],
                              i.meter_lock_value,
                              i.meter_lock_size,
                              &i.meter_lock_null);
    n++;

    // meter_in_service
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.meter_in_service_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.meter_in_service_value.capacity ());
    b[n].length = &i.meter_in_service_size;
    b[n].is_null = &i.meter_in_service_null;
    n++;

    // meter_reg_date
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.meter_reg_date_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.meter_reg_date_value.capacity ());
    b[n].length = &i.meter_reg_date_size;
    b[n].is_null = &i.meter_reg_date_null;
    n++;
  }

  void access::object_traits_impl< ::meter, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::meter, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // meter_id
    //
    if (sk == statement_insert)
    {
      long unsigned int const& v =
        o.meter_id;

      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.meter_id_value, is_null, v);
      i.meter_id_null = is_null;
    }

    // meter_unique
    //
    {
      ::std::string const& v =
        o.meter_unique;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.meter_unique_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.meter_unique_value,
        size,
        is_null,
        v);
      i.meter_unique_null = is_null;
      i.meter_unique_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.meter_unique_value.capacity ());
    }

    // meter_customer
    //
    {
      ::boost::shared_ptr< ::customer > const& v =
        o.meter_customer;

      typedef object_traits< ::customer > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::customer > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.meter_customer_value, is_null, id);
        i.meter_customer_null = is_null;
      }
      else
        i.meter_customer_null = 1;
    }

    // meter_owner
    //
    {
      ::std::string const& v =
        o.meter_owner;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.meter_owner_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.meter_owner_value,
        size,
        is_null,
        v);
      i.meter_owner_null = is_null;
      i.meter_owner_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.meter_owner_value.capacity ());
    }

    // meter_number
    //
    {
      ::std::string const& v =
        o.meter_number;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.meter_number_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.meter_number_value,
        size,
        is_null,
        v);
      i.meter_number_null = is_null;
      i.meter_number_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.meter_number_value.capacity ());
    }

    // meter_intital_unit
    //
    {
      ::std::string const& v =
        o.meter_intital_unit;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.meter_intital_unit_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.meter_intital_unit_value,
        size,
        is_null,
        v);
      i.meter_intital_unit_null = is_null;
      i.meter_intital_unit_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.meter_intital_unit_value.capacity ());
    }

    // meter_joinging_price
    //
    {
      ::std::string const& v =
        o.meter_joinging_price;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.meter_joinging_price_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.meter_joinging_price_value,
        size,
        is_null,
        v);
      i.meter_joinging_price_null = is_null;
      i.meter_joinging_price_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.meter_joinging_price_value.capacity ());
    }

    // meter_lock
    //
    {
      ::std::string const& v =
        o.meter_lock;

      bool is_null (true);
      if (mysql::enum_traits::set_image (
            i.meter_lock_value,
            i.meter_lock_size,
            is_null,
            v))
        grew = true;

      i.meter_lock_null = is_null;
    }

    // meter_in_service
    //
    {
      ::std::string const& v =
        o.meter_in_service;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.meter_in_service_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.meter_in_service_value,
        size,
        is_null,
        v);
      i.meter_in_service_null = is_null;
      i.meter_in_service_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.meter_in_service_value.capacity ());
    }

    // meter_reg_date
    //
    {
      ::std::string const& v =
        o.meter_reg_date;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.meter_reg_date_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.meter_reg_date_value,
        size,
        is_null,
        v);
      i.meter_reg_date_null = is_null;
      i.meter_reg_date_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.meter_reg_date_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::meter, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // meter_id
    //
    {
      long unsigned int& v =
        o.meter_id;

      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        v,
        i.meter_id_value,
        i.meter_id_null);
    }

    // meter_unique
    //
    {
      ::std::string& v =
        o.meter_unique;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.meter_unique_value,
        i.meter_unique_size,
        i.meter_unique_null);
    }

    // meter_customer
    //
    {
      ::boost::shared_ptr< ::customer >& v =
        o.meter_customer;

      typedef object_traits< ::customer > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::customer > > ptr_traits;

      if (i.meter_customer_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.meter_customer_value,
          i.meter_customer_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // meter_owner
    //
    {
      ::std::string& v =
        o.meter_owner;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.meter_owner_value,
        i.meter_owner_size,
        i.meter_owner_null);
    }

    // meter_number
    //
    {
      ::std::string& v =
        o.meter_number;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.meter_number_value,
        i.meter_number_size,
        i.meter_number_null);
    }

    // meter_intital_unit
    //
    {
      ::std::string& v =
        o.meter_intital_unit;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.meter_intital_unit_value,
        i.meter_intital_unit_size,
        i.meter_intital_unit_null);
    }

    // meter_joinging_price
    //
    {
      ::std::string& v =
        o.meter_joinging_price;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.meter_joinging_price_value,
        i.meter_joinging_price_size,
        i.meter_joinging_price_null);
    }

    // meter_lock
    //
    {
      ::std::string& v =
        o.meter_lock;

      mysql::enum_traits::set_value (
        v,
        i.meter_lock_value,
        i.meter_lock_size,
        i.meter_lock_null);
    }

    // meter_in_service
    //
    {
      ::std::string& v =
        o.meter_in_service;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.meter_in_service_value,
        i.meter_in_service_size,
        i.meter_in_service_null);
    }

    // meter_reg_date
    //
    {
      ::std::string& v =
        o.meter_reg_date;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.meter_reg_date_value,
        i.meter_reg_date_size,
        i.meter_reg_date_null);
    }
  }

  void access::object_traits_impl< ::meter, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::meter, id_mysql >::persist_statement[] =
  "INSERT INTO `meter` "
  "(`meter_id`, "
  "`meter_unique`, "
  "`meter_customer`, "
  "`meter_owner`, "
  "`meter_number`, "
  "`meter_intital_unit`, "
  "`meter_joinging_price`, "
  "`meter_lock`, "
  "`meter_in_service`, "
  "`meter_reg_date`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::meter, id_mysql >::find_statement[] =
  "SELECT "
  "`meter`.`meter_id`, "
  "`meter`.`meter_unique`, "
  "`meter`.`meter_customer`, "
  "`meter`.`meter_owner`, "
  "`meter`.`meter_number`, "
  "`meter`.`meter_intital_unit`, "
  "`meter`.`meter_joinging_price`, "
  "CONCAT(`meter`.`meter_lock`+0,' ',`meter`.`meter_lock`), "
  "`meter`.`meter_in_service`, "
  "`meter`.`meter_reg_date` "
  "FROM `meter` "
  "WHERE `meter`.`meter_id`=?";

  const char access::object_traits_impl< ::meter, id_mysql >::update_statement[] =
  "UPDATE `meter` "
  "SET "
  "`meter_unique`=?, "
  "`meter_customer`=?, "
  "`meter_owner`=?, "
  "`meter_number`=?, "
  "`meter_intital_unit`=?, "
  "`meter_joinging_price`=?, "
  "`meter_lock`=?, "
  "`meter_in_service`=?, "
  "`meter_reg_date`=? "
  "WHERE `meter_id`=?";

  const char access::object_traits_impl< ::meter, id_mysql >::erase_statement[] =
  "DELETE FROM `meter` "
  "WHERE `meter_id`=?";

  const char access::object_traits_impl< ::meter, id_mysql >::query_statement[] =
  "SELECT\n"
  "`meter`.`meter_id`,\n"
  "`meter`.`meter_unique`,\n"
  "`meter`.`meter_customer`,\n"
  "`meter`.`meter_owner`,\n"
  "`meter`.`meter_number`,\n"
  "`meter`.`meter_intital_unit`,\n"
  "`meter`.`meter_joinging_price`,\n"
  "CONCAT(`meter`.`meter_lock`+0,' ',`meter`.`meter_lock`),\n"
  "`meter`.`meter_in_service`,\n"
  "`meter`.`meter_reg_date`\n"
  "FROM `meter`\n"
  "LEFT JOIN `customer` AS `meter_customer` ON `meter_customer`.`customer_id`=`meter`.`meter_customer`";

  const char access::object_traits_impl< ::meter, id_mysql >::erase_query_statement[] =
  "DELETE FROM `meter`";

  const char access::object_traits_impl< ::meter, id_mysql >::table_name[] =
  "`meter`";

  void access::object_traits_impl< ::meter, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im.meter_id_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.meter_id = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::meter, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.meter_id);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::meter, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::meter, id_mysql >::pointer_type
  access::object_traits_impl< ::meter, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::meter, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::meter, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.meter_id);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::meter, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::meter, id_mysql >::object_type >
  access::object_traits_impl< ::meter, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += "\n";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        true,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::meter, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
