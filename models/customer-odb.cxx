// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "customer-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // customer
  //

  const char alias_traits<  ::branch,
    id_mysql,
    access::object_traits_impl< ::customer, id_mysql >::customer_branch_tag>::
  table_name[] = "`customer_branch`";

  struct access::object_traits_impl< ::customer, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::customer, id_mysql >::id_type
  access::object_traits_impl< ::customer, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::customer, id_mysql >::id_type
  access::object_traits_impl< ::customer, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        id,
        i.customer_id_value,
        i.customer_id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::customer, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // customer_id
    //
    t[0UL] = 0;

    // customer_unique
    //
    if (t[1UL])
    {
      i.customer_unique_value.capacity (i.customer_unique_size);
      grew = true;
    }

    // customer_branch
    //
    t[2UL] = 0;

    // customer_name
    //
    if (t[3UL])
    {
      i.customer_name_value.capacity (i.customer_name_size);
      grew = true;
    }

    // customer_gender
    //
    if (t[4UL])
    {
      i.customer_gender_value.capacity (i.customer_gender_size);
      grew = true;
    }

    // customer_contact
    //
    if (t[5UL])
    {
      i.customer_contact_value.capacity (i.customer_contact_size);
      grew = true;
    }

    // customer_address
    //
    if (t[6UL])
    {
      i.customer_address_value.capacity (i.customer_address_size);
      grew = true;
    }

    // customer_house_number
    //
    if (t[7UL])
    {
      i.customer_house_number_value.capacity (i.customer_house_number_size);
      grew = true;
    }

    // customer_reg_date
    //
    if (t[8UL])
    {
      i.customer_reg_date_value.capacity (i.customer_reg_date_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::customer, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // customer_id
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONGLONG;
      b[n].is_unsigned = 1;
      b[n].buffer = &i.customer_id_value;
      b[n].is_null = &i.customer_id_null;
      n++;
    }

    // customer_unique
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.customer_unique_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.customer_unique_value.capacity ());
    b[n].length = &i.customer_unique_size;
    b[n].is_null = &i.customer_unique_null;
    n++;

    // customer_branch
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.customer_branch_value;
    b[n].is_null = &i.customer_branch_null;
    n++;

    // customer_name
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.customer_name_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.customer_name_value.capacity ());
    b[n].length = &i.customer_name_size;
    b[n].is_null = &i.customer_name_null;
    n++;

    // customer_gender
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.customer_gender_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.customer_gender_value.capacity ());
    b[n].length = &i.customer_gender_size;
    b[n].is_null = &i.customer_gender_null;
    n++;

    // customer_contact
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.customer_contact_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.customer_contact_value.capacity ());
    b[n].length = &i.customer_contact_size;
    b[n].is_null = &i.customer_contact_null;
    n++;

    // customer_address
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.customer_address_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.customer_address_value.capacity ());
    b[n].length = &i.customer_address_size;
    b[n].is_null = &i.customer_address_null;
    n++;

    // customer_house_number
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.customer_house_number_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.customer_house_number_value.capacity ());
    b[n].length = &i.customer_house_number_size;
    b[n].is_null = &i.customer_house_number_null;
    n++;

    // customer_reg_date
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.customer_reg_date_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.customer_reg_date_value.capacity ());
    b[n].length = &i.customer_reg_date_size;
    b[n].is_null = &i.customer_reg_date_null;
    n++;
  }

  void access::object_traits_impl< ::customer, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::customer, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // customer_id
    //
    if (sk == statement_insert)
    {
      long unsigned int const& v =
        o.customer_id;

      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.customer_id_value, is_null, v);
      i.customer_id_null = is_null;
    }

    // customer_unique
    //
    {
      ::std::string const& v =
        o.customer_unique;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.customer_unique_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.customer_unique_value,
        size,
        is_null,
        v);
      i.customer_unique_null = is_null;
      i.customer_unique_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.customer_unique_value.capacity ());
    }

    // customer_branch
    //
    {
      ::boost::shared_ptr< ::branch > const& v =
        o.customer_branch;

      typedef object_traits< ::branch > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::branch > > ptr_traits;

      bool is_null (ptr_traits::null_ptr (v));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (v)));

        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_image (
          i.customer_branch_value, is_null, id);
        i.customer_branch_null = is_null;
      }
      else
        i.customer_branch_null = 1;
    }

    // customer_name
    //
    {
      ::std::string const& v =
        o.customer_name;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.customer_name_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.customer_name_value,
        size,
        is_null,
        v);
      i.customer_name_null = is_null;
      i.customer_name_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.customer_name_value.capacity ());
    }

    // customer_gender
    //
    {
      ::std::string const& v =
        o.customer_gender;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.customer_gender_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.customer_gender_value,
        size,
        is_null,
        v);
      i.customer_gender_null = is_null;
      i.customer_gender_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.customer_gender_value.capacity ());
    }

    // customer_contact
    //
    {
      ::std::string const& v =
        o.customer_contact;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.customer_contact_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.customer_contact_value,
        size,
        is_null,
        v);
      i.customer_contact_null = is_null;
      i.customer_contact_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.customer_contact_value.capacity ());
    }

    // customer_address
    //
    {
      ::std::string const& v =
        o.customer_address;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.customer_address_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.customer_address_value,
        size,
        is_null,
        v);
      i.customer_address_null = is_null;
      i.customer_address_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.customer_address_value.capacity ());
    }

    // customer_house_number
    //
    {
      ::std::string const& v =
        o.customer_house_number;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.customer_house_number_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.customer_house_number_value,
        size,
        is_null,
        v);
      i.customer_house_number_null = is_null;
      i.customer_house_number_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.customer_house_number_value.capacity ());
    }

    // customer_reg_date
    //
    {
      ::std::string const& v =
        o.customer_reg_date;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.customer_reg_date_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.customer_reg_date_value,
        size,
        is_null,
        v);
      i.customer_reg_date_null = is_null;
      i.customer_reg_date_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.customer_reg_date_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::customer, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // customer_id
    //
    {
      long unsigned int& v =
        o.customer_id;

      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_value (
        v,
        i.customer_id_value,
        i.customer_id_null);
    }

    // customer_unique
    //
    {
      ::std::string& v =
        o.customer_unique;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.customer_unique_value,
        i.customer_unique_size,
        i.customer_unique_null);
    }

    // customer_branch
    //
    {
      ::boost::shared_ptr< ::branch >& v =
        o.customer_branch;

      typedef object_traits< ::branch > obj_traits;
      typedef odb::pointer_traits< ::boost::shared_ptr< ::branch > > ptr_traits;

      if (i.customer_branch_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        mysql::value_traits<
            obj_traits::id_type,
            mysql::id_ulonglong >::set_value (
          id,
          i.customer_branch_value,
          i.customer_branch_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<mysql::database*> (db)->load<
            obj_traits::object_type > (id));
      }
    }

    // customer_name
    //
    {
      ::std::string& v =
        o.customer_name;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.customer_name_value,
        i.customer_name_size,
        i.customer_name_null);
    }

    // customer_gender
    //
    {
      ::std::string& v =
        o.customer_gender;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.customer_gender_value,
        i.customer_gender_size,
        i.customer_gender_null);
    }

    // customer_contact
    //
    {
      ::std::string& v =
        o.customer_contact;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.customer_contact_value,
        i.customer_contact_size,
        i.customer_contact_null);
    }

    // customer_address
    //
    {
      ::std::string& v =
        o.customer_address;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.customer_address_value,
        i.customer_address_size,
        i.customer_address_null);
    }

    // customer_house_number
    //
    {
      ::std::string& v =
        o.customer_house_number;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.customer_house_number_value,
        i.customer_house_number_size,
        i.customer_house_number_null);
    }

    // customer_reg_date
    //
    {
      ::std::string& v =
        o.customer_reg_date;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.customer_reg_date_value,
        i.customer_reg_date_size,
        i.customer_reg_date_null);
    }
  }

  void access::object_traits_impl< ::customer, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          long unsigned int,
          mysql::id_ulonglong >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::customer, id_mysql >::persist_statement[] =
  "INSERT INTO `customer` "
  "(`customer_id`, "
  "`customer_unique`, "
  "`customer_branch`, "
  "`customer_name`, "
  "`customer_gender`, "
  "`customer_contact`, "
  "`customer_address`, "
  "`customer_house_number`, "
  "`customer_reg_date`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::customer, id_mysql >::find_statement[] =
  "SELECT "
  "`customer`.`customer_id`, "
  "`customer`.`customer_unique`, "
  "`customer`.`customer_branch`, "
  "`customer`.`customer_name`, "
  "`customer`.`customer_gender`, "
  "`customer`.`customer_contact`, "
  "`customer`.`customer_address`, "
  "`customer`.`customer_house_number`, "
  "`customer`.`customer_reg_date` "
  "FROM `customer` "
  "WHERE `customer`.`customer_id`=?";

  const char access::object_traits_impl< ::customer, id_mysql >::update_statement[] =
  "UPDATE `customer` "
  "SET "
  "`customer_unique`=?, "
  "`customer_branch`=?, "
  "`customer_name`=?, "
  "`customer_gender`=?, "
  "`customer_contact`=?, "
  "`customer_address`=?, "
  "`customer_house_number`=?, "
  "`customer_reg_date`=? "
  "WHERE `customer_id`=?";

  const char access::object_traits_impl< ::customer, id_mysql >::erase_statement[] =
  "DELETE FROM `customer` "
  "WHERE `customer_id`=?";

  const char access::object_traits_impl< ::customer, id_mysql >::query_statement[] =
  "SELECT\n"
  "`customer`.`customer_id`,\n"
  "`customer`.`customer_unique`,\n"
  "`customer`.`customer_branch`,\n"
  "`customer`.`customer_name`,\n"
  "`customer`.`customer_gender`,\n"
  "`customer`.`customer_contact`,\n"
  "`customer`.`customer_address`,\n"
  "`customer`.`customer_house_number`,\n"
  "`customer`.`customer_reg_date`\n"
  "FROM `customer`\n"
  "LEFT JOIN `branch` AS `customer_branch` ON `customer_branch`.`branch_id`=`customer`.`customer_branch`";

  const char access::object_traits_impl< ::customer, id_mysql >::erase_query_statement[] =
  "DELETE FROM `customer`";

  const char access::object_traits_impl< ::customer, id_mysql >::table_name[] =
  "`customer`";

  void access::object_traits_impl< ::customer, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im.customer_id_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.customer_id = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::customer, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.customer_id);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::customer, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::customer, id_mysql >::pointer_type
  access::object_traits_impl< ::customer, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::customer, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::customer, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.customer_id);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::customer, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::customer, id_mysql >::object_type >
  access::object_traits_impl< ::customer, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += "\n";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        true,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::customer, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
